** Note: Strategy pattern is useful when we have multiple algorithms for 
specific task and we want our application/client to be flexible to chose any of 
the algorithm at runtime for specific task. **